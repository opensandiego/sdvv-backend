
# Used to show the total amount contributed by an individual to a committee
type ContributorSummary {
  name: String # last, first
  sum: Int
  count: Int # number of contributions
}

type Query {
  contributorSummary(committeeName: String!, limit: Int): [ContributorSummary]
}
